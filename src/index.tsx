import React from 'react'
import ReactDOM from 'react-dom'
import Formulizer from './Formulizer'

ReactDOM.render(
  <React.StrictMode>
    <Formulizer />
  </React.StrictMode>,
  document.getElementById('root')
)
